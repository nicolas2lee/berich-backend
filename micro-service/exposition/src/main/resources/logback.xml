<configuration>
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
       <encoder>
           <Pattern>
               %date{yyyy-MM-dd HH:mm:ss.SSSXXX} | %-5level | %X{IP} | %X{HostName} | %X{X-B3-TraceId:-} | %X{X-B3-SpanId:-} | %X{Marker} | %c{36} | %msg%n
           </Pattern>
       </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="console"/>
    </root>
</configuration>